spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jan_price_checker_test
    username: postgres
    password: postgres
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
      separator: ^^

mybatis:
  mapper-locations: classpath:mapper/*.xml
